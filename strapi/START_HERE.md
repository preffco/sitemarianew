# 🚀 Как запустить Strapi

## Проблема
У вас установлен Node.js v23.11.0, а Strapi требует версию 18-20.

## Решение

### Вариант 1: Использовать скрипт запуска

```bash
cd strapi
./start.sh
```

### Вариант 2: Ручной запуск

1. Откройте новый терминал

2. Загрузите nvm и переключитесь на Node.js 20:
```bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && source "$NVM_DIR/nvm.sh"
nvm use 20
```

3. Проверьте версию Node.js (должна быть v20.x.x):
```bash
node --version
```

4. Перейдите в папку Strapi и запустите:
```bash
cd /Users/goretofff/Desktop/Проекты/mariasite/strapi
npm run develop
```

5. Дождитесь сообщения "Server started" или "Your admin panel is ready"

6. Откройте в браузере: http://localhost:1337/admin

## Если видите ошибки

- **TypeScript ошибки**: Убедитесь, что файл `tsconfig.json` удален или правильно настроен
- **Порт занят**: Остановите другие процессы на порту 1337
- **Зависимости**: Выполните `npm install` в папке strapi

## После успешного запуска

1. Создайте администратора на http://localhost:1337/admin
2. Настройте права: Settings → Users & Permissions → Roles → Public
3. Включите `find` и `findOne` для Publication
4. Создайте публикации через Content Manager

